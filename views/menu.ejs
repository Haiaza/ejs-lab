<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Menu</title>
  </head>
  <body>
    <h1>Full Menu</h1>
    <%- include('./partials/nav') %>
    <p><% menu.forEach(item => { %>
      <%= item.name %><br>
      <%= item.price %><br>
      <%= item.details %><br>
      <%= item.rating %> out of 5 stars<br><br>
    <% }) %></p>
  </body>
</html>
